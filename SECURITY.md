# Security

This repository is a public client-side web application.

- Do not commit API keys, passwords, access tokens, or private configuration.
- Prefer keyless/public APIs for frontend-only demos.
- If a credential is ever committed, remove it from the current code **and rotate/revoke it**, because Git history may still contain the old value.

For security-sensitive reports, avoid posting live credentials in a public issue.
